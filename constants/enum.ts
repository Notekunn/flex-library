export enum OrderStatus {
  CREATED = 'created',
  PURCHASED = 'purchased',
  CANCELLED = 'cancelled',
}

export enum UserRole {
  Administrator = 'administrator',
  Owner = 'owner',
  Staff = 'staff',
  Member = 'member',
}
